[project]
name = "state-merging"
version = "0.1.0"
description = "A generic implementation of state-merging inference algorithms for finite-state automata and transducers"
requires-python = ">=3.10"

[tool.black]
line-length = 100
target-version = ['py310', 'py311']
